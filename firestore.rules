rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // السماح بالقراءة والكتابة الكاملة لجميع مستخدمي التطبيق والـ APK
    match /{document=**} {
      allow read, write: if true;
    }
    
  }
}
